const multiplicar = (x) => {
    return x * 2;
}

console.log(multiplicar(2));




const multiplicarPorDois = x => x * 2; //Arrow function em  uma linha quando expressao for simples so um parametro pode fazer desta maneira

console.log(multiplicarPorDois(4));
