let a = 10;

function multiplicar(x,y) {
    let a = x * y;     //função
    
    if(a > 10) {  

        let a = 0;  //if
        
        console.log(a);
    }
    
    console.log(a);
}

console.log(a); // global

multiplicar(3,7);